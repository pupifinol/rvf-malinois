import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function Page() {
  return (
    <PlaceholderPage
      title="Equipment"
      phase="Phase F3 — F5"
      description="Separator, choke manifold, flare, tanks, EMGAD / EMMAD. State only — no controls (UI/UX §2 / §10)."
    />
  );
}
