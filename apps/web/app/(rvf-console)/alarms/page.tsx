import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function Page() {
  return (
    <PlaceholderPage
      title="Alarms"
      phase="Phase F4"
      description="ISA-18.2 alarm center: prioritised list, acknowledgement, context. Process and instrumentation alarms shown separately."
    />
  );
}
