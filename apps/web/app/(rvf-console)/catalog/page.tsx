import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function Page() {
  return (
    <PlaceholderPage
      title="Equipment catalog"
      phase="Phase F1 — F5"
      description="Reusable equipment registry (ADR-004). Each EMMAD / EMGAD has its sensors, Modbus registers, and canonical tag mapping captured once and reused per job."
    />
  );
}
